# Python dependencies for ttnn-eltwise-op-tester
# These are in addition to tt-metal dependencies

# For PDF report generation (markdown -> PDF via pandoc)
markdown>=3.3.0
jinja2>=3.0.0

# For plotting
matplotlib>=3.5.0
seaborn>=0.12.0
pandas>=1.5.0
numpy>=1.23.0

# For accuracy testing
scipy>=1.9.0
loguru>=0.6.0

# System dependencies for PDF generation:
# - pandoc: apt-get install pandoc (Linux) or brew install pandoc (macOS)
# - pdflatex: apt-get install texlive-full (Linux) or brew install basictex (macOS)
